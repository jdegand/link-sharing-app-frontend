import { PlatformDirective } from "./platform.directive";

describe("PlatformDirective", () => {
  it("should create an instance", () => {
    const directive = new PlatformDirective();
    expect(directive).toBeTruthy();
  });
});
